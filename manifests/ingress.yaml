apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myk8sapp-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myk8sapp-svc
            port:
              number: 80
